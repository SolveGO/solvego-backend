spring:
  datasource:
    url: jdbc:mysql://localhost:3306/solvego_test
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

jwt:
  secret: test-secret-key-test-secret-key-test-secret-key-123456